<Window x:Class="TransportCompanyIS.Views.ShipmentEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Перевозка" Height="300" Width="380" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Откуда" Grid.Row="0" VerticalAlignment="Center" />
        <TextBox Grid.Row="0" Grid.Column="1" Margin="8,0,0,8" Text="{Binding FromAddress}" />

        <TextBlock Text="Куда" Grid.Row="1" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Grid.Column="1" Margin="8,0,0,8" Text="{Binding ToAddress}" />

        <TextBlock Text="Статус" Grid.Row="2" VerticalAlignment="Center" />
        <TextBox Grid.Row="2" Grid.Column="1" Margin="8,0,0,8" Text="{Binding Status}" />

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Сохранить" Width="100" Margin="0,0,8,0" Click="OnSaveClick" />
            <Button Content="Отмена" Width="90" Click="OnCancelClick" />
        </StackPanel>
    </Grid>
</Window>
